# Core pipeline. Versions are floors, not pins: nothing here uses an API that has
# churned recently, and pinning would fight the user's existing environment for no
# benefit.
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
lifelines>=0.27          # Cox models and the concordance index
requests>=2.28           # Allen Institute downloads

# Testing
pytest>=7.0

# Optional — only needed to build a reference from a .h5ad single-cell atlas.
# The synthetic fixture path and the full benchmark run without it, so it is not
# imported at module scope anywhere.
# anndata>=0.9
