# Core pipeline. Versions are floors, not pins: nothing here uses an API that has
# churned recently, and pinning would fight the user's existing environment for no
# benefit.
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
lifelines>=0.27          # Cox models and the concordance index
requests>=2.28           # Allen Institute downloads

# Testing
pytest>=7.0

# Needed to build a reference from a .h5ad single-cell atlas. Not imported at module
# scope anywhere, so the synthetic path, the frozen signature and the whole ACS
# leaderboard still run without it.
#
# It is no longer really optional in practice: MuSiC, Bisque, DWLS and SCDC all consume
# CELL-LEVEL data, so without an atlas they cannot run even with their R packages
# installed, and the pipeline falls back to this project's reimplementations.
anndata>=0.10
