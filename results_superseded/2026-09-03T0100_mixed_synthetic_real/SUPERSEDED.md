# SUPERSEDED — do not cite any number in this directory

Quarantined 2026-09-03. This tree is a **mixture of two different runs** and no file in
it can be attributed to a single cohort.

## Evidence

| file | mtime | provenance |
|---|---|---|
| `anatomic/anatomic_report.json` | 01:00 | **SYNTHETIC** — `n_samples` 212, `n_tumors` 24, `n_permutations` 500 |
| `anatomic/acs_leaderboard.csv` | 01:00 | **SYNTHETIC** — every row reports `n_tumors` 24, `n_constraint_tumor_pairs` 134 |
| `anatomic/composition_by_tumor_structure.csv` | 01:00 | **SYNTHETIC** — tumour ids `IVY00`…`IVY23` |
| `anatomic/agreement_report.json` | 01:00 | **SYNTHETIC** — rho = -0.335 computed from the synthetic ACS scores |
| `estimates/ivygap_control_shuffled_signature.csv` | 01:00 | **SYNTHETIC** — 212 rows, sample ids `IVYS0000`… |
| `estimates/ivygap_<11 other methods>.csv` | 01:09–01:11 | **REAL** — 122 rows, Ivy GAP numeric sample ids |

The real Ivy GAP anatomic study is 122 samples / 10 tumours. `IVY00`…`IVY23` and
`IVYS0000` are generated by `scripts/run_all.py::_synthetic_ivygap`, whose default is
`n_patients=24`.

## What happened

A `--synthetic` run written before `config.use_synthetic_paths()` existed wrote into
`results/`. A real run started later (01:09) and was interrupted after
`control_random` (01:11:32) — before `control_shuffled_signature`, and therefore before
the ACS scoring stage ever executed. The 01:00 synthetic scores were left in place and
were never overwritten.

The real numbers transcribed by hand into `RESULTS.md` (122 samples, 10 tumours,
9 evaluable, 10,000 permutations) are the ones that describe real tissue. Nothing in
this directory does.

## Replaced by

`results/` (regenerated) and `results_full_database/`. See `RESULTS.md`.
